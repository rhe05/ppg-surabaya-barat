# FILE_MAP.md — Peta Isi File (baca ini, bukan seluruh file)

> Tujuan: menemukan titik kode yang relevan dengan **pencarian string penanda**
> (`grep`), bukan membaca file 7000+ baris. Nomor baris cepat basi — penanda
> tidak. Update peta ini jika menambah section/modul baru.

## Struktur repo

> ⚠️ **2026-07-18: Index.html DIPECAH** jadi 4 file via pola HtmlService
> template `<?!= include('...'); ?>` (`doGet` sekarang pakai
> `createTemplateFromFile('Index').evaluate()`, bukan `createHtmlOutputFromFile`
> lagi). Server tetap menggabungkan semuanya jadi SATU output HTML — bug
> `//` di ERROR_LOG.md #1 tetap berlaku, guardrail `tools/check_local.js`
> sudah discan ke keempat file.

| Path | Isi | Kapan disentuh |
|---|---|---|
| `13_AppsScript/Index.html` | SHELL tipis: `<head>`/`<body>` + 3x `<?!= include(...) ?>`, ±35 baris. **Bukan tempat cari kode CSS/HTML/JS lagi.** | Jarang (hanya struktur shell) |
| `13_AppsScript/Style_Main.html` | Seluruh CSS (isi `<style>...</style>`, ±1900 baris) | Perubahan tampilan/CSS |
| `13_AppsScript/Markup_Screens.html` | Seluruh HTML screens & modal (±2190 baris) | Tambah/ubah screen atau modal |
| `13_AppsScript/Script_Main.html` | Seluruh JS utama (isi `<script>...</script>`, ±3290 baris) | Tambah/ubah logika frontend |
| `13_AppsScript/Code.js` | Entry `doGet` (`createTemplateFromFile`), `include(filename)` (helper penggabung), login (`serverLogin`), sesi, `DEV_MODE_SKIP_LOGIN` | Auth/akses/struktur shell |
| `13_AppsScript/Modul_Utilities.gs` | `SHEET_NAMES`, `readSheetAsObjects`, `findRowByQuery` (compare via String — ERROR_LOG #2), `updateRowByQuery`, `deleteRowByQuery`, `getCurrentUser`, `validateUserAccess`, **`withScriptLock_` (wajib untuk semua mutasi — ERROR_LOG #5)**, **cache: `cacheGet_`/`cachePut_`/`cacheDrop_`** (kunci: `guru_k<id>`, `santri_k<id>`) | Helper DB/RBAC/lock/cache |
| `13_AppsScript/Setup_Database.gs` | Skema semua sheet + `migrateGuruSchemaAddFields_` + `migrateSantriSchemaAddFields_` + `migrateJadwalKbmSchemaAddFields_` + `migrateUsersSchemaAddFields_` (⚠️ perlu run manual `setupDatabaseStructure()` di editor Apps Script tiap tambah kolom/sheet) — **BELUM DIJALANKAN untuk sheet baru `guru_izin` (2026-07-27)** | Perubahan skema |
| `13_AppsScript/Modul_MaintainGuru.gs` | CRUD guru (`serverGetGuruList/Add/Update/Delete`) | Fitur guru |
| `13_AppsScript/Modul_MaintainSantri.gs` | CRUD santri/generus + `serverBulkImportSantri` | Fitur generus |
| `13_AppsScript/Modul_Export.gs` | Ekspor xlsx asli via `Utilities.zip` (`serverExportGuruKelpXlsx`, `serverExportSantriKelpXlsx`, `buildXlsxBase64_`) | Fitur ekspor |
| `13_AppsScript/Modul_MaintainJadwalKBM.gs` / `Modul_MaintainPengumuman.gs` | CRUD Jadwal KBM & Pengumuman (Dashboard Kelompok) — "Kelas Pengajian" = Jadwal KBM diperkuat (santri_count, status Aktif/Tidak Aktif, KPI ringkasan `jkKpi*`, toolbar filter `#jadwalKbmSearchInput/FilterGuru/FilterStatus`, deteksi bentrok guru `cekKonflikJadwalGuru_`) | — |
| `13_AppsScript/Modul_MaintainSiklusGenerus.gs` | CRUD Siklus Generus (Data Master, sheet baru `siklus_generus`) — riwayat fase generus (Kerja/Kuliah/Pindah/Mondok/Tugas/Tidak Aktif), terikat `santri_id` existing (bukan bikin generus baru), `JENIS_SIKLUS_GENERUS_` | Fitur Siklus Generus |
| `13_AppsScript/Modul_InputAbsen.gs` | Screen Input Absen khusus role='guru' (dikunci total, tidak lihat shell admin) — kelas guru = `jadwal_kbm.kelas` milik `guru_id` yg terhubung ke `users.guru_id`; CRUD absen per kelas (`serverGetKelasAbsenList/serverGetAbsensiKelasForm/serverSaveAbsensiKelas`) + izin akses kelas guru lain per-tanggal (sheet baru `akses_kelas_request`: `serverListKelasUntukPermintaan/serverRequestAksesKelas/serverGetIncomingAksesRequests/serverGetMyAksesRequests/serverRespondAksesRequest`) + **Guru Izin** (tombol "Guru Izin" di header, sheet baru `guru_izin`, TANPA approval — sifatnya dokumentasi): `serverSubmitGuruIzin` (jenis harian/cuti, tanggal_mulai/selesai, alasan_kategori sakit/lainnya) + `serverGetGuruIzinAlasanSuggestions` (datalist alasan "Lainnya" yg pernah diketik, dibagi lintas guru) — PLUS mode Admin PPG (bebas pilih Kelompok/Kelas mana pun via sidebar "Input Absen", TIDAK dapat tombol Guru Izin): `serverGetInputAbsenKelompokOptionsAdmin/serverGetKelasAbsenListAdmin/serverGetAbsensiKelasFormAdmin/serverSaveAbsensiKelasAdmin`/`getAllKelasInKelompok_` (fungsi terpisah, tidak menyentuh RBAC guru) | Fitur Input Absen guru + Admin |
| `13_AppsScript/Code.js` → `serverRegisterGuru`/`serverCompleteOnboardingGuru`/`serverGetOnboardingKelompokOptions`/`serverResetPasswordSelfGuru`/`verifyGuruIdentity_` | Daftar mandiri (tab "Daftar", HANYA email+password) + wizard onboarding SETELAH login pertama (pilih peran Guru/Admin → Kelompok → Nama → Kelas) yang memverifikasi ke data Guru+Jadwal KBM (`ONBOARDING_ACTIVE_KELOMPOK_IDS_` = kelompok yg ditampilkan, saat ini hanya Kelp Petemon) | Fitur Daftar & Onboarding Guru |
| `13_AppsScript/Modul_MaintainPengurus.gs` | Simpan Data Pengurus (Data Master, sheet baru `pengurus_kelp`, DI ATAS Data Guru) — 9 jabatan tetap (`JABATAN_PENGURUS_`, disebut "Dapukan" di UI). Default 1 orang per jabatan (UPSERT via jabatan match saat `data.id` kosong), KECUALI `MULTI_HOLDER_JABATAN_` (`Wk Pembina Generus Kelp`) yang boleh >1 orang — selalu baris baru saat tambah, edit HARUS kirim `data.id` biar update baris yg tepat (bukan match by jabatan lagi). Nama BEBAS diketik (saran datalist dari nama ortu generus + generus jenjang Remaja), field `mulai_dapukan` → "Lama Dapukan" via `calcDurasiDetail_` | Fitur Data Pengurus |
| `13_AppsScript/Modul_Dashboard.gs`, `Modul_Statistics.gs`, `Modul_Laporan.gs`, `Modul_UserManagement.gs`, dst | Fitur lama (Phase 1-7), jarang berubah | — |
| `tools/check_local.js` | Cek sintaks + guardrail SEBELUM deploy (otomatis via git pre-commit hook; salinan hook di `tools/pre-commit`) | Tiap sebelum commit |
| `tools/verify_served.js` | Ambil & validasi output server SETELAH deploy | Tiap layar putih/anomali |
| `ERROR_LOG.md` | Riwayat bug + penanganan — **baca duluan saat ada error** | Tiap ada bug baru |

## Navigasi frontend — cari string penanda ini (grep)

> Sejak dipecah (lihat catatan di atas): CSS di `Style_Main.html`, HTML di
> `Markup_Screens.html`, JS di `Script_Main.html`. Grep langsung ke file yang
> sesuai kategori penanda, JANGAN grep/baca `Index.html` untuk ini (isinya
> cuma shell).

### CSS → `13_AppsScript/Style_Main.html` (blok `<style>`, bagian atas file)
- `LOGIN SCREEN` · `APP LAYOUT` · `SIDEBAR: DESA/KELOMPOK TREE`
- `GLOBAL LOADING OVERLAY` — spinner tengah layar tema brass (boot/simpan/hapus/ekspor)
- `DASHBOARD GURU (per Kelompok` — KPI card premium Dashboard Kelompok
- `.gk-topright` — panel fixed kanan atas: tombol refresh (`.gk-refresh-btn`) + `.dash-header-admin-card` (teks "Admin" saja)
- `.gk-toast` — notifikasi singkat (dipakai setelah Refresh)
- `.gk-kpi-lp` — mini-stat L/P di SEBELAH angka besar KPI (bukan di bawah)
- `.sidebar-icon` — icon SVG line monochrome sidebar (⚠️ JANGAN pakai emoji, lihat memory design standard)
- `.sidebar-footer` — freeze/sticky bottom untuk tombol Keluar
- `.screen-wrapper.active > .dash-header + *` — ⚠️ blok konten inilah yang menggulir (header TIDAK ikut scroll) supaya scrollbar mulai di bawah header; `.dash-container` di-set full-width + padding auto agar scrollbar tetap di pojok saat sidebar diciutkan
- `CUSTOM DATE PICKER` — datepicker tanpa library
- `MODAL TAMBAH GURU / GENERUS` — layout form 2 kolom + section
- `TABEL DAFTAR GURU` — toolbar filter/search, badge, tombol aksi
- `.gk-table-scroll` — kontainer scroll tabel (max-height ≈5 baris) + `.gk-sticky-l1/l2/r` = freeze kolom No/Nama (kiri) & Aksi (kanan)
- `.gk-colpicker-*` — dropdown "Kolom" (pilih grup kolom yang tampil)
- `MODAL KONFIRMASI HAPUS` — modal hapus premium (spinner sekarang di `#globalLoadingOverlay`, bukan di tombol)
- `MODAL DETAIL GURU/GENERUS` — modal detail read-only
- `MOBILE OPTIMIZATION` — semua breakpoint responsive

### HTML → `13_AppsScript/Markup_Screens.html` (screens & modals)
- `id="screenLogin"` · `id="appLayout"` · `id="screenGuruDashboard"` (Dashboard Kelompok)
- `id="globalLoadingOverlay"` — spinner global, kontrol via JS di bawah
- Panel kanan atas GLOBAL (di `.app-content`, tampil di semua screen): `class="gk-topright"` → tombol refresh + card "Admin". Tidak ada lagi avatar/nama/role.
- `id="gkToast"` — elemen notifikasi singkat
- `<datalist id="dlNama|dlNamaOrtu|dlTempatLahir|dlKelurahan|dlKecamatan|dlKabupaten|dlProvinsi|dlKodePos">` — sumber autocomplete, diisi `populateAutocomplete_()`
- Header seksi: `id="dataGuruTitle"` · `id="dataGenerusTitle"`
- Tabel: `id="guruDashTableWrapper"` · `id="santriDashTableWrapper"` (⚠️ tabel generus letaknya SETELAH `id="generusJenjangRow"` = 5 kartu jenjang). `<thead>` KOSONG di markup (`id="guruDashTableHead"`/`santriDashTableHead`) — diisi JS dari model kolom.
- Modal (semua `id="modal..."`): `modalGuruKelp` (tambah/edit guru) ·
  `modalGenerusKelp` · `modalDeleteGuruKelp` · `modalDeleteGenerusKelp` ·
  `modalDetailGuruKelp` · `modalDetailGenerusKelp` · `modalEksporGuru` ·
  `modalEksporGenerus`
- `id="screenInputAbsen"` (screen Input Absen guru/admin) · `id="iaMenuBtn"` (hamburger kiri atas, tooltip "Menu Utama") → `id="iaMenuDropdown"` (Dashboard/Pilih Kelas/Guru Izin/Minta Akses/Keluar, Keluar paling bawah; 3 item tengah disembunyikan di mode admin) · `id="iaDashboardView"`/`id="iaDashboardCards"` (ringkasan kehadiran per kelas milik guru, default view saat buka Input Absen) · `id="iaKelasView"` berisi `id="iaKelasRow"` (pill selector kelas, auto-select) + body input absen · `id="modalGuruIzin"` (form Izin Harian/Cuti + alasan Sakit/Lainnya, sheet baru `guru_izin`) · `id="modalMintaAkses"`/`id="modalIncomingRequests"` (izin akses kelas guru lain)

### JavaScript → `13_AppsScript/Script_Main.html` (blok `<script>` utama; cari `window.<nama>`)
- Boot & auth: `window.onload` → `serverCheckDevMode` → `renderApp` · `handleLogin` · `verifySession`
- Spinner global (WAJIB dipakai untuk semua momen tunggu baru, jangan buat spinner lokal): `showGlobalLoading_(text)` / `hideGlobalLoading_()`
- Dashboard Kelompok load: `loadKelompokDashboard(kelompokId, nama, onDone, forceFresh)` → `loadKelompokDashboardGuru_` · `loadKelompokDashboardSantriKelas_` (keduanya terima `(id, onDone, forceFresh)`) · `loadKelompokDashboardAbsen_`
- KPI (dipakai loader server DAN update lokal instan): `renderGuruKpis_` · `renderSantriKpis_` · `applyGuruMutationLocal_` · `applyGenerusMutationLocal_`
- **Model kolom tabel** (satu sumber untuk tampilan + ekspor): `GURU_COLS` · `SANTRI_COLS` (tiap kolom `{key,label,group,sticky,center,action,val,html}`), grup tampil: `guruVisibleGroups` · `generusVisibleGroups`
- Render tabel generik: `renderDataTable_(cols, list, theadId, tbodyId, emptyText)` · `visibleCols_` · `colStickyClass_` · `filterListByScopeSearch_`
- Column picker: `toggleColPicker_` · `setColGroup_`
- Tabel guru: `toggleGuruDashTable_` · `filterGuruDashTable_` · `renderGuruDashTable_`
- Tabel generus: `toggleGenerusDashTable_` · `filterSantriDashTable_` · `renderSantriDashTable_`
- Siklus Generus (Data Master, sheet `siklus_generus`, generus existing via nama+`dlSiklusGenerusNama`): `loadSiklusGenerusList_` · `toggleSiklusGenerusDashTable_` · `filterSiklusGenerusDashTable_` · `renderSiklusGenerusDashTable_` · `renderSiklusGenerusKpis_` · `openModalSiklusGenerus`/`editSiklusGenerus`/`saveSiklusGenerus`/`deleteSiklusGenerus` · `confirmEksporSiklusGenerus`.
- Pendidikan Formal (Data Master, TIDAK ADA sheet baru — cuma pakai field `pendidikan` yang sudah ada di sheet `santri`, lewat `serverUpdateSantri`): `renderPendidikanFormalKpis_` (5 kartu PAUD-TK/SD/SMP/SMA/Kuliah + L/P via `gk-kpi-lp`) · `togglePendidikanFormalDashTable_` · `renderPendidikanFormalDashTable_` · `openModalPendidikanFormal`/`editPendidikanFormal`/`savePendidikanFormal` · `confirmEksporPendidikanFormal`. Opsi dropdown pendidikan `generusKelpPendidikan` (modal Tambah/Edit Generus) ditambah `Kuliah` supaya tidak mismatch dgn field ini.
- Data Pengurus (Data Master, sheet baru `pengurus_kelp`, DI ATAS Data Guru — bukan pilih generus existing, nama bebas diketik, saran via `dlPengurusNama`): `loadPengurusList_` · `togglePengurusDashTable_` · `renderPengurusDashTable_` (kolom Dapukan/Nama/Mulai Dapukan/Lama Dapukan/Keterangan/Aksi) · `renderPengurusKpis_` (isi tiap kartu jabatan dari `PENGURUS_JABATAN_MAP_`; dapukan di `MULTI_HOLDER_JABATAN_` gabung semua nama dipisah koma) · `populatePengurusNamaDatalist_` · `updatePengurusLama_` (badge "Lama Dapukan", pola sama `updateGuruKelpLama`) · `openModalPengurus`/`editPengurus`/`savePengurus` (kirim `id` eksplisit ke server — WAJIB utk dapukan multi-holder spy tidak salah sasaran baris)/`deletePengurus`/`deletePengurusFromModal_` (tombol Hapus di footer form, tampil hanya mode edit) · `viewPengurus`/`closeModalDetailPengurus` (modal detail, "Lama Dapukan" otomatis) · `confirmEksporPengurus`.
- **Struktur Pengurus** (bagan CSS-only, tombol di toolbar Daftar Pengurus): `openModalStrukturPengurus`/`closeModalStrukturPengurus`/`buildStrukturPengurusChartHtml_` (bangun HTML chart, dipakai layar & PDF)/`renderStrukturPengurusChart_` (render ke layar + panggil fit)/`fitStrukturPengurusChart_` (skalakan via `transform:scale` biar SELALU utuh 1 layar tanpa scroll, diukur via `scrollWidth/clientWidth`, re-fit saat window resize)/`strukturNodeHtml_` (kotak polos)/`strukturColHtml_` (kolom + kaki garis, HANYA dipakai di dalam `.struktur-branch-row`). Hierarki: Pembina Generus → Wk Pembina Generus (1-4 kotak sejajar, dapukan multi-holder) → PJP Kelp → Kepsek/Pembina Pra Remaja/Pembina Remaja[→ Ketua Muda-Mudi bertumpuk di kolom yg sama]/Sekretaris/Bendahara. Garis horizontal `.struktur-branch-row::before` inset kiri/kanan 86px (=setengah lebar `.struktur-node` 172px) → presisi mulai/berakhir di titik tengah kotak pertama/terakhir. Tombol "Unduh" di header modal → `openModalDownloadStrukturPengurus` (pilih kertas A4/Folio) → `printStrukturPengurusPdf_` (dokumen cetak terpisah, landscape, footer kiri credit app + logo `.brand-mark`, footer kanan "Waktu Unduh" tanggal+jam). Judul dinamis via `strukturPengurusTitleText_()` ("Struktur Pengurus Generus Kelompok <nama> <tahun berjalan>"), dipakai di judul modal (`#strukturPengurusTitle`) & PDF. Baris Wk Pembina Generus pakai class `.struktur-branch-row.converge-below` (garis horizontal DI BAWAH juga, bukan cuma di atas) supaya turunan ke PJP Kelp jelas nyambung ke "batang" Wk Pembina. ⚠️ Garis horizontal baris (`.struktur-branch-line`) TIDAK LAGI dihitung via CSS inset 86px (pernah meleset, berhenti sebelum kotak terakhir pada baris 5-kotak) — sekarang diukur NYATA oleh `drawStrukturBranchLines_(root)` (getBoundingClientRect kolom pertama/terakhir) lalu digambar sbg elemen `<div class="struktur-branch-line">` sungguhan, dipanggil SETELAH innerHTML disisipkan SEBELUM `fitStrukturPengurusChart_`. PDF (`printStrukturPengurusPdf_`) punya salinan logic pengukuran yg sama inline (window terpisah, tidak bisa akses fungsi window utama). Segmen Wk Pembina→PJP Kelp sekarang bercabang berupa SIKU (elbow, dipilih user dari 3 opsi visual): `drawWkPembinaElbow_(root)` menggambar turun dari kotak Wk Pembina yg NAMANYA COCOK dgn PJP Kelp (dicocokkan via `.struktur-node-name` text, fallback ke kolom pertama kalau tidak ada yg cocok — BUKAN asal kolom pertama, supaya tidak tergantung urutan data) → belok mendatar → turun lagi tepat di sumbu-tengah menuju `.struktur-connector` polos ke PJP Kelp (kalau cuma 1 kotak/tidak ada yg cocok namanya kebetulan sudah segaris tengah, otomatis jadi garis lurus tanpa siku). Elemen garis siku: `.struktur-elbow-line`/`.struktur-elbow-vline`/`.struktur-elbow-hline`. PDF (`printStrukturPengurusPdf_`) punya salinan logic yg sama persis.
- `setDataMasterFocus_` (Script_Main.html) generik lewat `window.DATA_MASTER_SECTIONS_` (pengurus/guru/generus/pendidikan/siklus) — buka Daftar salah satu section otomatis sembunyikan section lainnya.
- Autocomplete: `fillDatalist_` · `populateAutocomplete_` (dipanggil tiap modal guru/generus dibuka)
- Refresh: `hardRefresh_` (⚠️ DILARANG `location.reload()` — ERROR_LOG #6; pakai `forceFresh` menembus cache) · `SCREEN_LOADERS_` · `showToast_`
- CRUD guru (klien): `openModalGuruKelp` · `saveGuruKelp` (add/update via `currentEditGuruId`) · `editGuruKelp` · `viewGuruKelp` · `deleteGuruKelp` → `confirmDeleteGuruKelp`
- CRUD generus (klien): pola sama, ganti `Guru`→`Generus` (`currentEditGenerusId`, dst)
- Ekspor (mengikuti kolom yang dipilih di column picker): `confirmEksporGuru` · `confirmEksporGenerus` · `exportColsForData_` · `buildExportMatrix_` · `runXlsxExport_` → server `serverBuildXlsxFromData(token, sheetName, headers, rows)` · `downloadXlsxFromBase64_` · `printPdfList_`
- Datepicker: `toggleDatePicker` · `renderDatePicker` · `pickDate` (hook umur & lama mengajar di sini → `updateGuruKelpUmur` / `updateGuruKelpLama`)
- Util: `escapeHtmlExport_` · `digitsOnlyInput_` · `formatPhoneDashInput_` · `buildDetailItemHtml_` · ikon: `iconViewSvg_`/`iconEditSvg_`/`iconDeleteSvg_` (⚠️ dilarang ada `//` dalam string — ERROR_LOG #1)
- **Tanggal — WAJIB lewat `parseDateFlexible_`**: tanggal dari Sheet bisa datang sbg objek Date → string ISO, jadi JANGAN pernah `dateStr + 'T00:00:00'` (dulu bikin "NaN undefined"). Turunannya: `calcUsiaThn_` (usia) · `formatTanggalDisplay_` · `calcDurasiDetail_` ("X tahun Y bulan Z hari", dipakai Lama Mengajar)
- Input Absen guru (2026-07-27, redesign pill kelas + auto-select + Guru Izin): `initInputAbsen_` · `openInputAbsenAsAdmin_` · `iaOnTanggalChange_` (dispatcher: rute ke `iaLoadDashboardSummary_` atau `iaLoadKelasList_` sesuai `iaState_.activeView`) · `iaLoadKelasList_` (load kelas via spinner global, lalu AUTO-select kelas terakhir/pertama) · `iaSelectKelas_` (load santri via spinner global) — chip kelas pill 1-baris (`.ia-kelas-chip-nama` + badge `.ia-kelas-chip-count`) · Guru Izin: `iaOpenGuruIzin_`/`closeModalGuruIzin_`/`iaSetIzinJenis_` (harian/cuti)/`iaSetIzinAlasan_` (sakit/lainnya, toggle field detail)/`iaSubmitGuruIzin_` → server `serverSubmitGuruIzin`/`serverGetGuruIzinAlasanSuggestions`
- Menu Utama + Dashboard Input Absen (2026-07-27, redesign): `iaToggleMenu_`/`iaCloseMenu_` (dropdown hamburger) · `iaMenuGoDashboard_`/`iaMenuGoPilihKelas_` → `iaShowDashboardView_`/`iaShowKelasView_` (toggle `#iaDashboardView`/`#iaKelasView`, `iaState_.activeView`) · `iaMenuOpenGuruIzin_`/`iaMenuOpenMintaAkses_`/`iaMenuExit_` (bungkus fungsi lama, tutup menu dulu) · `iaLoadDashboardSummary_`/`iaRenderDashboardCards_` → server `serverGetGuruDashboardSummary`/`serverGetGuruDashboardSummaryRange` (Modul_InputAbsen.gs, ringkasan hadir/izin/sakit/alpa per kelas milik guru — 1 tanggal ATAU rentang via filter, `iaState_.dashboardFilter`) · info kelas per card pakai `iaDurasiMenit_` (durasi menit dari jam) + label "Cabe Rawit" kalau `item.kategori === 'Cabe Rawit'` (field `kategori` dari `getKelasSessionInfo_`, sumber `jadwal_kbm.kategori`)
- Header Input Absen (2026-07-28, restyle hijau + split layout): `.ia-topbar` (putih: hamburger + brand "Ruang Ngaji" + lonceng) di atas `.ia-header-hero` (gradient hijau `--sage`→`--brand-green`: nama guru+tanggal + icon kalender kanan, otomatis sejajar lurus di bawah lonceng). `toggleDatePicker(inputId, triggerElId)` sekarang terima trigger id kustom (dipakai tombol kalender/filter, bukan cuma input teks). `iaUpdateDateLabel_` isi `#iaHeaderDateLabel`. Filter rentang tanggal Dashboard: `iaToggleFilterPanel_`/`iaApplyFilter_`/`iaResetFilter_` (`#iaFilterBtn`/`#iaFilterPanel`, pill premium kanan atas di bawah header)
- ⚠️ Perf "Pilih Kelas" (2026-07-28, ERROR_LOG #18-#21): `santri`/`guru`/`jadwal_kbm` Kelp Petemon (`kelompok_id: '1'`) sudah pindah Firestore (`FIRESTORE_KELOMPOK_TABLES_`, Modul_Utilities.gs) — JANGAN pakai `readSheetAsObjects(SANTRI/GURU/JADWAL_KBM)` generik di Modul_InputAbsen.gs (baca Sheets PENUH + Firestore, 2x kerja sia-sia utk kelompok yg sudah migrasi). Pakai `iaReadKelompokTable_(sheetName, kelompokId)` (1 tabel) atau `iaReadKelompokTablesParallel_([...], kelompokId)` (>1 tabel Firestore sekaligus via `UrlFetchApp.fetchAll`, paralel bukan berurutan). `getKelasOwnedByGuru_`/`getKelasSessionInfo_` terima param opsional array-yang-sudah-dibaca (`jadwalRowsAll`/`guruRowsAll`, fallback ke `iaReadKelompokTable_`) — SELALU baca dulu baru panggil fungsi ini. `serverGetKelasAbsenList`/`serverGetKelasAbsenListAdmin` terima `preferKelas`, return `formKelas`+`formData` sekalian (gabung 2 round-trip client↔server jadi 1) — `iaLoadKelasList_` (Script_Main.html) pakai `result.formData` langsung kalau cocok, skip panggil `serverGetAbsensiKelasForm` terpisah. Cache client-side (ERROR_LOG #21): `iaDashboardCacheKey_`/`iaKelasCacheKey_` + `iaState_.dashboardLoadedKey`/`kelasLoadedKey` — `iaShowDashboardView_`/`iaShowKelasView_` skip `google.script.run` total kalau kunci (tanggal/filter/kelompokId) sama dengan load terakhir (DOM lama TIDAK dibersihkan saat view disembunyikan, cuma display:none). Diputus di `saveInputAbsen_` setelah simpan sukses.
- Kehadiran Generus (2026-07-28, tab Operasional Kelp; redesain kartu 2026-07-30; filter bulan/tahun + matrix detail 2026-07-30): `serverGetKehadiranGenerusKategori` (Modul_Monitoring.gs) — % kehadiran PER BULAN PILIHAN (bukan cuma bulan berjalan lagi) dikelompokkan per KATEGORI JADWAL KBM (`KATEGORI_JADWAL_`, dari `jadwal_kbm.kategori`) — BEDA dari `serverGetMonitoringGenerus` (tab Monitoring terpisah, tidak disentuh) yang mengelompokkan via `santri.jenjang_saat_ini`. UI: `#kgKategoriGrid` FLAT per kelas gaya KPI card `.guru-dash-kpi-card.kg-simple-card` (sama seperti Data Guru/Generus), diurutkan via `window.KG_KELAS_ORDER_`/`kgKelasSortIndex_` (PAUD/TK A→Remaja SMA, 8 kelas — "Muda Mudi" DIHAPUS dari urutan 2026-07-30), render lewat `renderKehadiranGenerusGrid_`. Filter bulan/tahun kartu `#kgFilterBulan`/`#kgFilterTahun` (sebelah tombol Ekspor) → `window.loadKelompokDashboardAbsen_` via helper generik `window.kgInitFilterSelect_(bulanId, tahunId)` (auto-default bulan berjalan sekali, dipakai juga oleh filter Detail & Ekspor). Header section 3 tombol (pola sama Data Guru): "+ Kehadiran" → `openModalInputKehadiranGenerus`/`saveIkgForm_` (modal admin override manual, reuse `serverGetAbsensiForm`/`serverSaveAbsensiDaily` — WAJIB merge dgn seluruh santri kelompok saat simpan krn `serverSaveAbsensiDaily` hapus-total+insert-ulang per tanggal); icon "Kehadiran" → `toggleKgDetailTable_`/`loadKgDetailTable_`/`renderKgDetailTable_` — tabel MATRIX premium (santri × tiap hari efektif Senin-Jumat bulan pilihan, ~20 kolom, sticky nama+kelas kiri & sticky header atas via CSS `.kg-matrix-*`, badge huruf H/I/S/A berwarna per sel), filter kelas `#kgDetailKelasFilter` + filter bulan/tahun sendiri `#kgDetailBulan`/`#kgDetailTahun`, sumber `serverGetKehadiranGenerusMatrix` (Modul_Monitoring.gs baru, return `{dates, data:[{nama,kelas,statusByDate}]}`, kolom weekday-only dihitung server-side); "Ekspor" → `confirmEksporKehadiranGenerus` (ikut filter bulan/tahun KARTU, bukan filter Detail) pakai `serverGetKehadiranGenerusDetailList` (flat, TETAP dipakai khusus utk ekspor CSV/PDF — beda dari matrix yg dipakai tabel Detail) + `printPdfList_`/`runXlsxExport_`.
- Laporan Perkembangan Santri — Section A Kehadiran (2026-07-30, tab Laporan Kelp, sebelumnya "Segera hadir"): `serverGetLaporanPerkembanganSantri(token, kelompokId, guruId, year, month)` (Modul_Laporan.gs) — santri milik guru ditentukan via `jadwal_kbm` aktif punya guru itu → daftar kelas → `santri.kelas_ngaji` cocok (santri TIDAK punya kolom guru_id langsung, pola sama `serverGetKehadiranGenerusKategori`). Status per-santri (field `status`, prioritas berurutan): `Hadir` (%hadir≥80) → `Izin` (ada hari izin) → `Alfa` (ada hari alfa) → `Sakit` (sisanya) → `Belum Ada Data` (0 record bulan itu, dikecualikan dari rata-rata `hadirPercent`). UI: toolbar `#lpsGuruSelect`/`#lpsBulan`/`#lpsTahun` + tombol "Buat Laporan" (`window.loadLaporanPerkembanganSantri_`) — guru select TERHUBUNG ke Jadwal KBM (2026-07-30, lalu dikelompokkan per kategori 2026-07-30): diisi dari `window.lastJadwalListData` (guru YANG PUNYA kelas aktif saja, bukan seluruh roster Data Guru — `window.populateLpsGuruSelect_`, dibangun ulang tiap buka tab biar ikut edit Jadwal KBM terbaru di sesi yg sama), dikelompokkan via `<optgroup>` per KATEGORI persis kartu Jadwal KBM (urutan `window.KATEGORI_JADWAL_UI_`: Cabe Rawit/Pra Remaja SMP/Remaja SMA/Muda-Mudi), tiap opsi label-nya "Nama — kelas1, kelas2" (kelas HANYA dari kategori itu; guru yg ngajar >1 kategori muncul di tiap optgroup terkait, value tetap guru_id yg sama). Pilih guru → `window.onLpsGuruChange_` render `#lpsGuruInfo` (chip per kelas + % dari `window.lastKehadiranGenerusData_`, silang-cek cepat ke kartu Kehadiran Generus tab Operasional sebelum klik Buat Laporan — HANYA info bantu, TIDAK dipakai backend, hitungan laporan tetap dari `serverGetLaporanPerkembanganSantri` independen). Init tab di `switchKelpSectionTab_` cabang `'Laporan'`. Render: `window.lpsBuildBodyHtml_(data)` (shared builder dipakai baik utk preview layar `#laporanPerkembanganResult` maupun PDF, HINDARI logic ganda) — layout 2026-07-30: judul + periode DI TENGAH atas (`.lps-header-center`), lalu blok info RATA KIRI (`.lps-info-block`, baris Guru/Kelas/Jadwal KBM/Ruangan — `kelasInfo` dari backend, kalau guru pegang >1 kelas tiap baris format "kelas: nilai" dipisah `;`), baru 4 kartu metrik flat `.lps-metric-card` (Kehadiran/Izin/Alfa/Sakit, warna hex `window.LPS_STATUS_WARNA_HEX_`) + tabel detail `.lps-detail-table` (Nama/Hari Aktif/Kehadiran/Status, badge warna per status). PDF (2026-07-30, GANTI TOTAL dari window.print() ke pdfmake — lihat entri terpisah di bawah): `window.unduhLaporanPerkembanganSantriPdf_`/`lpsBuildAndDownloadPdf_`.
- PDF pdfmake + Kop Surat (2026-07-30, tab Laporan Kelp): "Unduh PDF" TIDAK PAKAI `window.print()` lagi — `pdfmake.min.js`+`vfs_fonts.js` CDN (Index.html, sebelum `include('Script_Main')`, pola sama Google Charts loader) generate PDF 100% di klien (`window.lpsBuildAndDownloadPdf_(data, kop)` susun `docDefinition` pdfmake: kop opsional → judul+periode center → info block → 4 "kartu" metrik pakai `canvas rect` sbg garis atas berwarna (pdfmake tak punya border-top asli) → `table` detail dgn status berwarna). Kop Surat = letterhead (logo + 3 BARIS TEKS TIPOGRAFI-PER-BARIS 2026-07-30: Baris1=Nama TPQ/TPA, Baris2=No. Izin Operasional, Baris3=Alamat, masing² punya font/bold/ukuran/warna/tata-letak sendiri + garis pemisah global) diatur admin lewat modal "Kop Surat" (`window.openModalKopSurat_`/`saveKopSurat_`/`loadKopSuratForm_`, field per baris id pola `ksB1Teks`/`ksB1Font`/`ksB1Bold`/`ksB1Ukuran`/`ksB1Warna`/`ksB1Align` dst utk B2/B3, default tipografi di `window.KS_BARIS_DEFAULTS_` HARUS sinkron dgn atribut default di markup; upload logo di-*resize* client-side via `<canvas>` maks sisi 480px lalu di-ekspor JPEG (bukan PNG) dgn `quality` diturunkan bertahap sampai base64 < `window.KS_MAX_LOGO_BASE64_LEN_` (~350rb karakter) `window.onKsLogoFileChange_` — ⚠️ ERROR_LOG-worthy bug 2026-07-30: PNG lossless dari logo detail bisa tembus 1-2 MB base64, payload sebesar itu bikin `google.script.run` gagal DIAM-DIAM (success/failure handler sama sekali tidak terpanggil) shg tombol "Simpan" kelihatan tidak berfungsi — makanya WAJIB dikompres+dibatasi, bukan cuma di-resize dimensi. `saveKopSurat_` skrg dibungkus try/catch + tombol Simpan disabled+"Menyimpan..." saat proses biar gagal apapun kelihatan, tidak diam lagi) — DISIMPAN SELALU DI FIRESTORE (`Modul_KopSurat.gs`, `serverGetKopSurat`/`serverSaveKopSurat`, path `kelompok/{id}/kop_surat/{kategoriSlug}`, field FLAT `b1_teks`/`b1_font`/`b1_bold`/`b1_ukuran`/`b1_warna`/`b1_align` dst — `firestoreEncodeFields_` TIDAK dukung nested object/array jadi TIDAK BOLEH kirim `baris: [...]`, TIDAK PERNAH Sheets krn base64 logo bisa lampaui batas 1 sel ~50rb karakter tapi aman di bawah limit 1 MiB/dokumen Firestore). Render PDF: `window.lpsEnsurePdfFonts_` daftarkan `pdfMake.fonts` sekali (Roboto dari vfs_fonts.js CDN + Helvetica/Times/Courier via nama font standar bawaan pdfkit TANPA embed tambahan — 4 pilihan ini di dropdown `ksB{n}Font` HARUS sama persis dgn `window.KS_PDF_FONTS_`), `docDefinition.defaultStyle.font` WAJIB diisi ('Roboto') begitu `pdfMake.fonts` custom di-set. MVP kategoriSlug HARDCODE `window.KOP_SURAT_KATEGORI_ = 'cabe-rawit'` — kop surat baru diikutkan ke PDF kalau kelas guru yg dipilih ada di kategori Jadwal KBM "Cabe Rawit" (`window.lpsGuruKategoriList_`, cross-check via `window.lastJadwalListData` yg sudah di memori, TANPA round-trip baru), kategori lain nanti tinggal ditambah pilihan UI tanpa ubah backend (struktur sudah kategori-scoped). Firestore HANYA di-fetch pas klik Unduh PDF & kop relevan (bukan tiap render laporan) — hemat kuota baca. Layout PDF 2026-07-30: `pageMargins: [56.7, 42.5, 56.7, 40]` (kiri/kanan 2cm, atas 1.5cm, bawah tetap 40pt — 1cm≈28.35pt), lebar garis kop `x2:482` menyesuaikan area konten baru (bukan 515 spt margin lama). Logo `width:80` (naik dari 48, HANYA `width` diisi tanpa `height` biar pdfmake jaga aspect ratio, logo non-persegi tidak gepeng). Checkbox baru `ksGarisAtas`/field `garis_atas` ("Tambah Garis Kop Surat", di bawah checkbox garis pemisah existing) — garis TAMBAHAN di ATAS logo/teks kop (garis existing `pakai_garis` tetap di BAWAH kop), warna sama-sama ikut Baris 1.
- ⚠️ PERF Kehadiran Generus + Laporan (2026-07-30): `serverGetKehadiranGenerusKategori`/`Matrix`/`DetailList` (Modul_Monitoring.gs) DAN `serverGetLaporanPerkembanganSantri` (Modul_Laporan.gs) diganti dari `readSheetAsObjects()` generik (scan SELURUH sheet semua kelompok lalu filter) ke `iaReadKelompokTablesParallel_`/`iaReadKelompokTable_`/`iaReadAbsensiKelompok_` (Modul_InputAbsen.gs — SCOPED ke satu kelompok, Firestore langsung utk Kelp Petemon via `FIRESTORE_KELOMPOK_TABLES_`). Fungsi `ia*` sengaja dipanggil lintas-file (GAS satu namespace global, sudah lazim di codebase ini) — JANGAN duplikasi logicnya. `serverGetMonitoringGenerus` (tab Monitoring terpisah) SENGAJA tidak diubah, di luar fokus sesi ini.
- ⚠️ Perf "Simpan Absen" (2026-07-28, ERROR_LOG #22): `iaRewriteAbsensiKelas_(kelompokId, santriIdsKelas, tanggal, absensiList, dicatatOleh)` — GANTI TOTAL cara `serverSaveAbsensiKelas`/`Admin` menulis (dulu: N× `deleteRowByQuery` + M× `generateId`, MASING-MASING scan ulang SELURUH sheet absensi sambil pegang `LockService.getScriptLock()` GLOBAL). Sekarang: 1× baca + hitung di memori + 1× `clearContent`+`setValues` batch. Otomatis pakai `iaRewriteAbsensiKelasFirestore_` (hapus+buat dokumen paralel via `fetchAll`) kalau kelompoknya sudah migrasi Firestore — `appendRowToSheet`/`deleteRowByQuery` GENERIK **tidak** tahu `FIRESTORE_KELOMPOK_TABLES_`, jangan pernah pakai langsung utk tabel per-kelompok, harus percabangan manual spt `Modul_MaintainSantri.gs`.

## Alur deploy & verifikasi (baku)

```
node tools/check_local.js          # 1. cek lokal
git add <file> && git commit && git push   # 2. auto-deploy via GitHub Actions
gh run watch <id> --exit-status    # 3. tunggu deploy sukses
node tools/verify_served.js        # 4. WAJIB: validasi output server
```
