# ── Dependencies ──
node_modules/
**/node_modules/

# ── Next.js / build output ──
.next/
out/
build/
dist/

# ── Environment & secrets ──
.env
.env.*
!.env.example

# ── clasp credentials (JANGAN pernah di-commit!) ──
.clasprc.json
**/.clasprc.json

# ── Extract engine service account key (JANGAN pernah di-commit!) ──
service-account-key.json
*-service-account*.json
extracted_data.json

# ── Logs & OS junk ──
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# ── Prisma local db ──
*.db
*.db-journal
