# ── Dependencies ──
node_modules/
**/node_modules/

# ── Next.js / build output ──
.next/
out/
build/
dist/

# ── Environment & secrets ──
.env
.env.*
!.env.example

# ── clasp credentials (JANGAN pernah di-commit!) ──
.clasprc.json
**/.clasprc.json

# ── Extract engine service account key (JANGAN pernah di-commit!) ──
service-account-key.json
*-service-account*.json
extracted_data.json
transformed_data.json

# ── Supabase credentials (JANGAN pernah di-commit!) ──
supabase_key.txt
*supabase_key*

# ── Contains real user row samples incl. password_hash — JANGAN di-commit ──
schema_mapping_report.json

# ── Logs & OS junk ──
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# ── Prisma local db ──
*.db
*.db-journal
